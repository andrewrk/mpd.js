{
  "name": "mpd",
  "version": "0.0.1",
  "description": "javascript interface to music player daemon that works in node.js and the browser",
  "main": "lib/mpd.js",
  "engines": {
    "node": "~0.8.0"
  },
  "scripts": {
    "dev": "npm install && coco -wco lib/ src/",
    "build": "npm install && coco -co lib/ src/",
    "clean": "rm -rf lib/"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:superjoe30/node-mpd.git"
  },
  "keywords": [
    "mpd",
    "music",
    "player",
    "daemon"
  ],
  "author": "Andrew Kelley",
  "license": "BSD",
  "devDependencies": {
    "coco": "~0.8.0"
  }
}
